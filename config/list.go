package config

var (
	Columns              []string // --columns, -C
	AllColumns           bool     // --all-columns, -A
	SortBy               = ""     // --sort-by, -S
	ReverseSortDirection bool     // --reverse, -R
)
